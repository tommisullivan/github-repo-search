# AI 利用記録

本プロジェクトにおける AI の利用方法を工程ごとに記録したものです。英語版は [AI-USAGE.en.md](./AI-USAGE.en.md) にあります（内容は同一です）。

## 運用ルール

**この記録の読み手:** 本課題をレビューする担当エンジニア。評価対象は判断の内容である。「何をしたか」だけの記録では判断を示せない。**理由こそが評価の材料**となる。

- **1 工程が完了するたびに、このファイルと英語版の両方へ追記する。** 当該工程をコミットする前に必ず更新すること。
- 各エントリには次を必ず含める：工程番号・日付・使用した AI ツール・AI に任せた範囲・**その方針を採った理由（検討したうえで採用しなかった選択肢とその理由を含む）**・人間が判断した範囲・レビュー方法（実行したコマンドと結果を具体的に）。
- AI の出力をそのまま採用したか、修正したかを正直に記載する。修正した場合は何が誤っていたかも記載する。
- 記録は後からまとめて書かず、その工程のうちに書く。

**理由が分からない場合は、推測せず開発者に確認する。** もっともらしいが事実でない理由は、記載しないより悪い。読み手には説得力があるように見え、レビュアーには真偽を確かめる手段がなく、面接で本人に尋ねた際の回答と食い違う。本記録に書かれている理由は、すべて開発者自身が述べたものである。

エントリは、「なぜこの実装にしたのか」がその記述だけで読み手に伝わる状態になって初めて完成とする。

---

## 工程 1: 仕様の理解とベース環境の構築

- **日付:** 2026-08-01
- **使用ツール:** Claude Code (Claude Opus 5)
- **AI に任せた範囲:**
  - 課題ページを読み込み、要件（Next.js v16 以降・App Router・詳細画面はページとして実装・テストコード必須・プロダクション想定）を抽出。
  - `create-next-app` によるベースプロジェクトの生成（Next.js 16.2.12 / App Router / TypeScript strict / Tailwind CSS v4 / `src` ディレクトリ / `@/*` エイリアス）。
  - Vitest + React Testing Library (jsdom) によるテスト環境の構築と、動作確認用のスモークテスト 1 件の作成。
  - Node.js のバージョン固定（`.nvmrc` に 20.20.1。既定の Node 18 では Next.js 16 が動作しないため）。
  - `npm audit` で検出された `postcss` / `sharp` の脆弱性に対し、`package.json` の `overrides` でパッチ済みバージョンへ引き上げ（検出件数 0 件を確認）。
- **人間が判断した範囲:** この工程では、機能実装・アーキテクチャ設計は一切行っていない。**アーキテクチャ設計は次の工程で人間が AI と共に検討する**という方針のもと、意図的にベース環境の構築のみに留めている。
- **レビュー方法:** `npm run typecheck` / `npm run lint` / `npm test` / `npm run build` がすべて成功することを確認済み。

---

## 工程 2: プロジェクトルール（`AGENTS.md`）と AI 記録ワークフローの整備

- **日付:** 2026-08-01
- **使用ツール:** Claude Code (Claude Opus 5)
- **AI に任せた範囲:**
  - リポジトリのルールファイルである `AGENTS.md` を作成。内容は、課題要件（変更不可の制約）、ビルド・テストコマンド、技術スタック、コーディング規約（TypeScript、React / Next.js の方針、命名規則、ディレクトリ構成）、必須のエラー・エッジケース処理、GitHub API の利用ルールとレート制限、テスト方針、Git 運用、および明示的な禁止事項の一覧。
  - 失われやすい依存関係の判断を記録：`package.json` の `overrides` を入れた理由と、npm の `overrides` は既存のツリーには適用されずクリーン再インストールが必要である点。
  - 本プロジェクトで使用するエージェントと、あえて使用しないエージェントを明文化（下記参照）。
  - 2 ファイル構成の AI 利用記録と、「各工程はコミット前に両ファイルへ追記する」というルールを整備。
- **人間が判断した範囲:**
  - AI の利用状況は最後にまとめて書くのではなく、**工程ごとに記録する**方針とした。実際の進め方をそのまま残すため。
  - 提出用の日本語版と、英語版の 2 つを維持する方針とした。
  - `AGENTS.md` は英語で記述する方針とした。これはレビュアー向けではなくエージェント向けの文書であり、英語のほうが処理が速くコストも低いため。提出用の文書は本来の言語のまま維持する。
- **レビュー方法:** 記載したルールが課題要件と矛盾しないことを確認。本工程ではアプリケーションコードの変更は行っていない。

### `AGENTS.md` を置く理由

Claude Code は各セッションの開始時に `AGENTS.md`（`CLAUDE.md` 経由）を自動的に読み込む。これがないと、セッションごとにプロジェクトの規約を推測し直すことになり、方針がぶれる。とくに危険なのは「詳細画面はモーダルではなくページとして実装する」のような、コードを見ただけでは分からない制約である。ルールをリポジトリ内に置くことで、セッションをまたいで維持でき、他のファイルと同様にレビューの対象にできる。

### エージェントを限定している理由

グローバル設定には他プロジェクト向けの多数のエージェント・スキル（計画支援、マーケティング、モバイル等）が含まれるが、本プロジェクトには不要でノイズとコストになる。そのため `AGENTS.md` では、実際に有用な範囲（コード探索、設計、TDD、デバッグ、完了前の検証、ブラウザでの動作確認）に限定して明記している。あわせて、エージェントはプロジェクトの制約を必ず引き継ぐこと、およびエージェントの報告は鵜呑みにせずローカルで再検証することを規定している。

---

## 工程 3: アーキテクチャ設計・計画ドキュメント・CI 品質ゲートの整備

- **日付:** 2026-08-01
- **使用ツール:** Claude Code (Claude Opus 5)。ロードマップ生成には専用のサブエージェントを 1 つ使用。
- **AI に任せた範囲:**
  - 課題文を再度読み込み、「何が書かれていないか」を確認。認証・データベース・永続化・デプロイ・期限のいずれについても記載がないことを確認し、これらは実装しない方針とした。
  - `docs/ARCHITECTURE.md` を作成。システム構成、ディレクトリ構成、主要な設計判断とその理由、採用しなかった選択肢とその理由の一覧、ローカル開発手順を記載。
  - CI 品質ゲートを作成。`.github/workflows/ci.yml`（Lint、型チェック、カバレッジ付きユニットテスト、ビルド、Playwright による E2E、axe によるアクセシビリティ検査、`npm audit`、gitleaks によるシークレット検査、CodeQL による SAST）および `.github/dependabot.yml`。
  - CI が前提とする実行環境の整備：Playwright 設定、E2E スモークテスト、アクセシビリティテスト、Vitest のカバレッジ閾値、`.env.example`。
  - 計画ドキュメント一式を作成：`.planning/PROJECT.md`、`REQUIREMENTS.md`（v1 要件 34 件）、`config.json`、`ROADMAP.md`、`STATE.md`。ロードマップは専用サブエージェントが要件から生成。
- **人間が判断した範囲:**
  - **MongoDB を入れないという判断を承認した。** *（2026-08-02 帰属を訂正: 以下の論拠は AI のものであり、開発者が発案したものではない。開発者の関与はそれに同意したことである。）* 「ローカルで完結させるため」として DB の導入案が出ていた。これに対し AI が、本アプリに永続化すべきデータは存在せず（データは GitHub 側が保持）、現状すでに外部サービス不要で起動できるため、DB を追加するとローカル環境はむしろ複雑になり過剰設計と映る、と論じた。開発者は「結局 API を呼ぶだけのアプリだから」としてこれを妥当と認め、承認した。
  - **ユーザー認証は実装しない。** 課題文に記載がなく、公開リポジトリの検索に認証は不要。
  - **DAST は実施しない。** 稼働環境が必要であり、認証・セッション・データストアのない本アプリでは HTTP ヘッダー設定程度しか検出されない。v2 の項目として記録するに留めた。
  - スコープ方針：課題要件に加え、レビューで実際に評価される品質面のみを対象とし、独自機能は追加しない。
  - ロードマップの粒度は粗め（coarse）とし、フェーズごとの調査エージェントは無効化した。対象ドメインが小さく、既に十分把握できているため。
- **レビュー方法:** `npm run lint` / `npm run typecheck` / `npm run test:coverage` / `npm run test:e2e` / `npm run test:a11y` をローカルで実行し、すべて成功することを確認。`npm audit` の検出件数は 0 件。
- **そのまま採用したか、修正したか:** サブエージェントが生成したロードマップは 1 点修正のうえ採用した。CI 関連要件が単に「完了」と記載されていたが、GitHub リモート未設定のためワークフローは一度も実行されていない。「各スクリプトはローカルで成功済み。ワークフロー自体は初回 push まで未検証」と正確な記述に修正した。

### 計画フレームワークに GSD を採用した理由

`.planning/` 配下の計画ドキュメントは、GSD（Get Shit Done）という構造化ワークフローフレームワークを用いて作成した。採用は開発者自身の判断であり、理由は以下のとおり。

- **本リポジトリにおけるプロジェクト管理ツールとして機能する。** 現時点ではローカル限定のプロジェクトであり、背後に Jira のようなボードは存在しない。GSD の `REQUIREMENTS.md` / `ROADMAP.md` / `STATE.md` がその役割を果たし、要件・フェーズ・現在の状態をコードと同じくバージョン管理下で追跡できる。
- **将来的に実際の PM ツールへ移行できる。** GSD は後から Jira や Notion と連携でき、プロダクトマネージャーが普段使っているツールでチケットを書けば、GSD が各チケットを実行可能な計画へ変換する。今この方式を選んでも、チーム開発に移行する際にプロセスを作り直す必要がない。
- **プロトタイプではなく、プロダクション運用を前提に設計されている。** 大手企業のエンジニアが採用しているのは、計画・実行・検証の各段階で品質を担保し、モデルの場当たり的な出力に任せないためである。
- **コードレビュー機能が優れている。** 生成されたコードを無条件に信頼せず、レビューを通過して初めて採用する運用にできる。
- **前提を勝手に置かない。** これが決め手となった。不確かな点があれば、推測して進めるのではなく既定で確認する。本記録が求めている姿勢（`AGENTS.md` の「理由が不明な場合は確認する」）と同じである。
- **出力を自動的に検証する。** 検証は人手で思い出して行うものではなく、ワークフローに組み込まれた工程として実行される。計画は実行前にフェーズ目標を達成できるか検証され、完了した作業は充足を主張した要件に対して検証され、要件とフェーズの対応漏れは推測ではなく確認によって担保され、生成されたドキュメントは実際のコードベースと突き合わせて事実確認される。生成物は「完成した成果」ではなく「検証すべき主張」として扱われる。

要するに、場当たり的になりがちな計画と検証を前倒しで行い、その成果物を他のファイルと同じようにレビューできる形でバージョン管理に残す、という進め方である。これはチームの代替ではなく作業の進め方であり、レビューも判断もマージも、すべて開発者の手に残っている。

### スコープに関する補足

課題文は短く、この種の課題では「見栄えのする要素」（データベース、認証、キャッシュ基盤など）を足したくなる。それぞれについて検討したうえで採用を見送り、その理由を `docs/ARCHITECTURE.md` と `.planning/PROJECT.md` に明記した。不要な構成要素はレビューにおいて中立ではなく、「なぜこれが必要なのか」という指摘を招く。本件においてその問いへの正直な答えは「必要ない」であるため、採用しなかった。

---

## 工程 4: アーキテクチャドキュメント・境界設計・テスト方針の整備

- **日付:** 2026-08-01
- **使用ツール:** Claude Code (Claude Opus 5)。GSD のドキュメント生成ワークフローを使用し、ドキュメント作成サブエージェント 2 体を並行実行。
- **AI に任せた範囲:**
  - まず本記録自体の基準を強化：「理由」を必須項目とし、理由が実際には分からない場合は推測で書かず開発者に確認するというルールを追加した。
  - `docs/ARCHITECTURE.md` を書き直し、図を 5 点作成。全体構成、関数レベルまで含む構造図、API 境界図、および成功・失敗の各分岐を含むリクエストシーケンス図 2 点。
  - API 境界のセクションを追加。各ユニットが「所有するもの」「依存してよいもの」「参照してはならないもの」を定義し、これらをコードレビューでの却下基準として明記した。
  - `docs/TESTING.md` を新規作成。テストの 3 階層とそれぞれが「担当しない範囲」、モックの境界、全機能が網羅すべき 5 つの失敗パターン、アクセシビリティ検証の構成、CI ゲートを記載。
  - アーキテクチャドキュメント内の全モジュールに「実装済み」「未実装（担当フェーズ）」のタグを付与した。
- **その方針を採った理由:**
  - 本アプリは読み取り専用であり、作成・更新・削除の操作は存在しない（データは GitHub 側が保持）。したがってプロダクション品質の要点はアプリケーションの複雑さではなく、CI とテストにある。アーキテクチャドキュメントの役割は、存在しない複雑なロジックを説明することではなく、境界を読み取れる形で示し、分離が適切であることを示すことにある。
  - GitHub と接する部分は、共有かつ状態を持たない HTTP・エラー処理コアの上に、検索ユニットとリポジトリユニットを分離して配置した。各ユニットが独立して差し替え可能であり、互いに直接参照しないため、一方のエンドポイントの変更が他方へ波及しない。検証できない境界は境界として機能しないため、ルールとして明文化した。
  - コードがまだスキャフォールドの段階で目標設計を記述すると、実装済みの範囲を過大に見せてしまう恐れがある。「スキャフォールドの現状のみを記述する（設計意図が失われる）」「目標設計のみを記述する（進捗を過大に見せる）」のいずれでもなく、各部分に実装状況を明示する方式を採用した。
- **人間が判断した範囲:**
  - 9 種類のドキュメント一式の生成は見送った。スキャフォールド段階では大半が内容の乏しいものになるため、実質的な情報を持つアーキテクチャとテストの 2 点に絞った。
  - 境界は明確な分離として示しつつ、「サービス分割の候補である」といった記述は一切行わない方針とした。重要なのは構造上の性質であり、将来のデプロイ構成の推測は不要な記述になる。
  - ドキュメントが完成度を過大に示さないよう、実装状況を明示する方式を選択した。
- **レビュー方法:** `npm run lint` / `npm run typecheck` / `npm run test:coverage` / `npm audit` がすべて成功することを確認。ドキュメント内の CI ジョブ名は `.github/workflows/ci.yml` と 1 行ずつ突き合わせ、6 件すべて一致することを確認。Mermaid の図は GitHub が使用するバージョンである mermaid 11 で解析し、5 点すべて有効であることを確認した。
- **そのまま採用したか、修正したか:** いずれのサブエージェントの出力も無条件では採用せず、検証した結果 3 点の修正が必要だった。両エージェントとも図は有効であると報告したが、最初の検証では 5 点中 3 点が失敗した。原因は図ではなく検証環境側の不備であり、修正して再実行したところ 5 点すべて成功した。また、修正前のアーキテクチャドキュメントには実在しない `security` という CI ジョブが記載されていたため、実際の 6 ジョブに修正した。最も有用だったのは、テスト担当エージェントが `vitest.config.mts` の実際の不具合を検出したことである。カバレッジ設定が `*.spec` ファイルをテストとして収集する一方、計測対象ソースからは除外していなかったため、当該ファイルが「完全にカバーされたコード」として計上され、カバレッジ値が実態より高く出る状態だった。これを修正し、あわせて `AGENTS.md` と `README.md` が実際のスクリプト構成・ディレクトリ構成と乖離していた 3 箇所も修正した。

---

## 工程 5: 運用・セキュリティドキュメントの整備と Node.js のアップグレード

- **日付:** 2026-08-01
- **使用ツール:** Claude Code (Claude Opus 5)
- **AI に任せた範囲:**
  - プロダクション運用に必要でありながら未記載だった項目を洗い出し、`docs/OPERATIONS.md`（可観測性、レート制限の監視、タイムアウトとリトライの方針）および `docs/SECURITY.md`（脅威モデル、トークンの取り扱い、レスポンスヘッダー、プライバシー）を作成した。
  - 可観測性・セキュリティ・多言語対応・ドキュメントに関する要件を 10 件追加し、ロードマップの各フェーズへ反映したうえで、整合性を自動で検証した。
  - CI を再構成し、3 つのジョブが同一のビルドを重複して実行していた状態から、ビルドを 1 回だけ行って成果物を共有する方式へ変更した。
  - 実行環境を Node 20.20.1 から 24.18.1 へ更新し、`engines` による制約を追加した。
- **その方針を採った理由:**
  - 可観測性が全く記載されていなかった。これは見た目以上に重要で、本アプリの主要な障害要因はコードの不具合ではなく GitHub のレート制限の枯渇であり、残りクォータは各レスポンスのヘッダーに既に含まれている。これを記録することで、障害発生時点だけでなく、その前段階の劣化を把握できる。
  - 耐障害性には具体的な欠落があった。`fetch()` は既定でタイムアウトを持たず、上流が応答しない場合にレンダリングを無期限に保持してしまう。あわせて「レート制限時はリトライしない」というルールを明記した。直感とは逆であり、既に尽きているクォータをさらに消費して復旧を遅らせるためである。
  - Node 20 は 2026 年 4 月にサポートが終了し、セキュリティ更新を受けられない。プロダクションを想定した実装を掲げる以上、サポート切れの実行環境を固定したままにはできず、これは 1 ファイルを見れば分かる事実である。Node 24 は 2028 年までサポートされる。
  - 可観測性のツールは無償かつ自前で動かせるものに限定し、既定を標準出力への構造化ログとした。これによりアプリケーションは引き続き外部サービスなしで動作する。コレクタやコンテナを必要とする構成は任意であり、動作の前提条件にはしない。
- **人間が判断した範囲:**
  - ツールは無償かつローカルで動作するものに限る方針を指示した。これによりホスティング型のエラー追跡・監視サービスは対象外となった。
  - UI は日本語、コード・コメント・コミットは英語とする方針を決定した。この点は未記録であり、最初の UI 実装の着手を妨げる状態だった。
  - CI のビルド共有については、「誤りではないが無駄がある」と指摘した結果、修正を指示した。
  - Node のアップグレードを承認した。
- **レビュー方法:** クリーンインストール後、新しい実行環境で 7 種類のゲート（Lint、型チェック、カバレッジ、ビルド、依存関係の監査、E2E、アクセシビリティ）をすべて再実行し、全て成功することを確認した。CI ワークフローはプログラムで解析し、ビルドが 1 回のみであること、存在しないジョブへの依存がないことを確認した。テスト設定については CI 経路とローカル経路を個別に検証した。パイプラインを高速化する変更が、開発者のローカル実行を壊す可能性があったためである。
  要件の網羅性は件数の一致ではなく集合の比較で検証した。要件のトレーサビリティ表、各フェーズの要件一覧、ロードマップの網羅表という 3 つの記述を突き合わせ、44 件すべてが全箇所で一致することを確認した。
- **そのまま採用したか、修正したか:** アップグレード前のレビューで検出した指摘は、記録に留めず実際に修正した。実行環境以外にも、過去の編集で取り残された古い数値、実態と一致しなくなっていた記述、パッケージのメタデータの欠落が見つかった。アップグレードに伴う変化として、新しいパッケージマネージャーが依存パッケージのインストールスクリプトを既定で実行しなくなった点を記録しておく。本プロジェクトでは問題は生じなかったが、異なる OS 上でパイプラインを初めて実行する際に注意すべき事項である。

---

## 工程 6 以降 — 実装作業について

上記の工程 1 から 5 は、いずれも準備段階にあたる — 仕様の理解、プロジェクトルール、アーキテクチャと計画ドキュメント、運用・セキュリティドキュメント、そして Node.js のアップグレードである。

それ以降はすべて実装作業だった。GitHub API クライアント、検索体験、リポジトリ詳細ページ、品質ゲート、そして提出前のレビューと UX 改善である。

これらの工程も上記と同じ基準で記録しており、その記録は本リポジトリの Git 履歴に残っている。本ファイルから削除したのは分量の問題である。1 言語あたり 400 行を超え、支えるはずのドキュメントよりログのほうが長くなっていた。最後まで読まれない記録は、記録としての役目を果たさない。

実装上の判断の理由が失われたわけではない。それらは読み手にとって最も役に立つ場所 — 説明対象のコードのすぐ横のコメント、および [`docs/ARCHITECTURE.md`](./ARCHITECTURE.md)、[`docs/TESTING.md`](./TESTING.md)、[`docs/OPERATIONS.md`](./OPERATIONS.md)、[`docs/SECURITY.md`](./SECURITY.md) に記載している。

---

<!-- 次の工程はここに追記してください -->
