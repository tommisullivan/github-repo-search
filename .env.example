# Optional. Raises GitHub API rate limits from ~10 to ~30 search requests/min,
# and core REST from 60/hour to 5,000/hour.
#
# SERVER-SIDE ONLY. Never prefix with NEXT_PUBLIC_ and never commit a real value.
# The app works fully without it.
GITHUB_TOKEN=
