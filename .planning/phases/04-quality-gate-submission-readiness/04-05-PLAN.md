---
phase: 04-quality-gate-submission-readiness
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - README.md
  - docs/OPERATIONS.md
  - docs/AI-USAGE.ja.md
  - docs/AI-USAGE.en.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-07, DOC-08]
user_setup: []

must_haves:
  truths:
    - "README.md is bilingual, Japanese first, and a reviewer reading only the README finds: setup, the optional server-side token, the key decisions with reasons (no database, no auth, subscribers_count, no DAST), and a self-contained AI usage summary"
    - "The subscribers_count/watchers_count trap is explained in the README — the STATE.md deferred DOC-01 note, finally landed where the brief asks"
    - "Both AI usage logs carry an identical Process 9 entry with real whys, and their process counts match"
    - "docs/OPERATIONS.md no longer defers route/requestId to a phase that has passed — the v1 disposition is recorded as a decision with reasons"
    - "All seven Definition of Done commands were run in this session and their output read; REQUIREMENTS/ROADMAP/STATE mark the phase complete; no merge was performed or attempted"
  artifacts:
    - path: "README.md"
      provides: "Bilingual submission-facing README (DOC-01, DOC-07, DOC-08)"
      contains: "subscribers_count"
      min_lines: 120
    - path: "docs/AI-USAGE.en.md"
      provides: "Process 9 entry, identical in content to the Japanese file's 工程9"
      contains: "Process 9"
    - path: "docs/AI-USAGE.ja.md"
      provides: "工程9 entry, identical in content to the English file's Process 9"
      contains: "工程"
    - path: "docs/OPERATIONS.md"
      provides: "route/requestId reconciliation per D4-18"
      contains: "requestId"
  key_links:
    - from: "README.md"
      to: "docs/AI-USAGE.ja.md and docs/AI-USAGE.en.md"
      via: "links from the AI usage summary section (DOC-02's README-link half)"
      pattern: "AI-USAGE"
    - from: "README.md"
      to: "src/lib/github/repo.ts"
      via: "the subscribers_count reasoning, consistent with the code-level comments a reviewer will cross-check"
      pattern: "subscribers_count"
---

<objective>
Make the repository explain itself to the person it was built for, then close the phase: the bilingual README with the decisions and the self-contained AI usage report the brief demands, the OPERATIONS.md reconciliation, Process 9 in both logs, the planning-doc closeout, and the full seven-command Definition of Done gate — run, read, and reported. Then stop: a human reviews and merges.

Purpose: DOC-01, DOC-02, DOC-07, DOC-08 are Phase 4 success criteria 5–7, and this plan is the phase's terminal gate (Definition of Done, all seven items). It runs last because the README describes what the other four plans shipped, and the gate must exercise the tree they left.

Output: rewritten README.md, reconciled docs/OPERATIONS.md, Process 9 in both AI usage logs, updated REQUIREMENTS.md/ROADMAP.md/STATE.md, and a clean seven-command gate log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md
@.planning/phases/04-quality-gate-submission-readiness/04-01-SUMMARY.md
@.planning/phases/04-quality-gate-submission-readiness/04-02-SUMMARY.md
@.planning/phases/04-quality-gate-submission-readiness/04-03-SUMMARY.md
@.planning/phases/04-quality-gate-submission-readiness/04-04-SUMMARY.md
@docs/AI-USAGE.en.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The bilingual README — Japanese first, decisions with reasons, self-contained AI summary</name>
  <files>README.md</files>
  <read_first>
    - README.md as it stands — what survives (if anything), what goes
    - .planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md § D4-15..D4-17 — single-file bilingual structure, the DOC-08 self-containment requirement (「利用方法のレポートをREADMEにまとめてください」— summarised IN the README), and the decision list
    - src/lib/github/repo.ts (the GitHubRepoDetailPayload comment) and src/components/RepoDetail.tsx (header comment) — the subscribers_count reasoning already written at the code; the README states the same trap in reviewer-facing terms, consistent with both
    - docs/AI-USAGE.en.md Processes 1–9 — the raw material the AI summary condenses (Process 9 may be drafted in Task 3; the summary section can reference the phase's work from the four plan summaries)
    - .env.example — what the token documentation must match
  </read_first>
  <acceptance_criteria>
    - Structure: Japanese section first, complete; explicit divider; English section second, same content. Every claim appears in both languages (DOC-07)
    - Japanese section covers, at minimum:
      1. What the app is and the two views (search `/`, detail `/repos/{owner}/{repo}`)
      2. Setup: `nvm use` (Node 24.18.1 — machine-default 18 cannot run Next 16), `npm install`, `npm run dev`; the full command table (test, coverage, e2e, a11y, lint, typecheck, build)
      3. The optional `GITHUB_TOKEN`: what it changes (rate limits: unauthenticated search ~10/min, core 60/h), server-side only, never `NEXT_PUBLIC_`, never committed, `.env.example` pointer (DOC-01)
      4. Key decisions, each with its reason: **no database / no auth** (nothing to persist, over-engineering, and the security dividend); **`subscribers_count` for watchers** — the REST API's `watchers_count` duplicates `stargazers_count`, so the brief's "watchers" comes from `subscribers_count` on the detail endpoint (the STATE.md deferred note — this is its landing place); **no DAST** (no auth/session/datastore to attack); **zero production dependencies beyond Next/React** (platform over library); **URL as state** (shareable, refreshable results)
      5. Error handling as a feature: the five states (loading, empty, network failure, rate limit with reset time, not found) each render distinctly
      6. The AI usage summary (DOC-08): self-contained — how AI was used across the project (planning docs, implementation, tests, per-process logging), what the human decided (assignment interpretation, dependency vetoes, merge authority, the decisions recorded as human calls in the logs), and how output was verified (the seven-command gate, per-phase, output read) — followed by links to `docs/AI-USAGE.ja.md` / `docs/AI-USAGE.en.md` for the per-process detail (DOC-02's README half)
    - The AI summary's claims about "what the human decided" are drawn from the existing log entries' Human decisions fields — never invented; if a why is not in the logs and not knowable, it is omitted rather than fabricated (AGENTS.md: never invent a rationale)
    - Tone: submission-facing, factual, no marketing; user-facing text Japanese-first per the language rule; code identifiers stay English in both halves
    - `npm run lint` and `npm run typecheck` exit 0
  </acceptance_criteria>
  <action>
Write the Japanese section first and translate faithfully to English — not the reverse — so the primary audience gets the primary text. Where the README summarises a decision documented deeper (ARCHITECTURE/SECURITY/OPERATIONS/TESTING), link to the doc rather than duplicating its argument; the README carries the conclusion and the one-line reason, the doc carries the evidence. The subscribers_count note is the exception: the brief asks for both stars and watchers, so the README explains the trap fully in place (that is why the note was deferred to this phase rather than split across commits — STATE.md's own reasoning).
  </action>
  <verify>
    <automated>grep -c "subscribers_count" README.md && grep -c "AI-USAGE" README.md && npm run lint && npm run typecheck</automated>
  </verify>
  <done>README is bilingual Japanese-first, self-sufficient for a reviewer, carries the four key decisions with reasons, the token documentation, and the self-contained AI usage summary with links to both logs.</done>
</task>

<task type="auto">
  <name>Task 2: docs/OPERATIONS.md — reconcile the route/requestId gaps as a recorded decision</name>
  <files>docs/OPERATIONS.md</files>
  <read_first>
    - docs/OPERATIONS.md § "What gets logged" — the `requestId` row ("Becomes per inbound request in Phase 2") and the `route` row ("Deferred to Phase 2") to reconcile
    - .planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md § D4-18 — the disposition: doc-only, because the call sites live in the sealed client
  </read_first>
  <acceptance_criteria>
    - The `requestId` row states the shipped v1 behaviour as a decision: per-GitHub-call, generated in the client, shared by a retry with its first attempt — with the reason (one upstream, one call per user action; per-call correlation is the only correlation the app has needed) and the honest note that per-inbound-request threading would require opening the sealed `src/lib/github/` boundary and is deferred to v2 if ever
    - The `route` row stops deferring to Phase 2: records that the field remains omitted by decision — the `endpoint` literal union already identifies which of the two calls logged, and with exactly two routes mapping 1:1 onto two endpoints, a `route` field would duplicate `endpoint` under another name
    - No stale forward reference to a completed phase remains anywhere in the two rows; no code is changed (the scope fence: `src/lib/github/**` sealed)
    - `npm run lint` and `npm run typecheck` exit 0
  </acceptance_criteria>
  <action>
Small, surgical edit in the document's voice. The point is not to excuse the gap but to convert a stale "will do later" into a recorded decision with its reasoning — the same move the document itself made for `cacheHit` and D-11a. If, while editing, the executor believes the code change is actually wanted: stop and report; it is a sealed-boundary decision, not an edit.
  </action>
  <verify>
    <automated>grep -n "Phase 2" docs/OPERATIONS.md | head -5 && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Both rows record shipped v1 behaviour as reasoned decisions; no stale deferrals; no code touched.</done>
</task>

<task type="auto">
  <name>Task 3: Process 9 in both logs, planning closeout, and the full Definition of Done gate</name>
  <files>docs/AI-USAGE.ja.md, docs/AI-USAGE.en.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - docs/AI-USAGE.en.md Process 8 — the format and register reference; the entry-count parity note at its tail
    - All four plan summaries (04-01..04-04) — the whys, measurements, and rejected alternatives Process 9 must carry: the E2E-mock decision ladder (page.route impossible server-side; T-01-26; MSW rejected), the CSP ladder and its measurements, the coverage red-run proof, the a11y findings
    - .planning/ROADMAP.md § Definition of Done — the seven items this task closes
  </read_first>
  <acceptance_criteria>
    - Process 9 (工程9) appended to BOTH logs with identical content, covering Phase 4: Delegated to AI / Why this approach (including rejected alternatives with reasons — the D4-01 and D4-05/06 ladders are the load-bearing ones) / Human decisions (drawn from real events this phase: anything the human chose or overrode; the LICENSE flag raised per D4-21; if a why is unknown, ASK the user before writing — never invent) / Review (the actual commands and their real results from this session) / Taken as-is vs modified (honest)
    - Entry-count parity holds: `grep -c '^## Process' docs/AI-USAGE.en.md` equals `grep -c '^## 工程' docs/AI-USAGE.ja.md`
    - The full gate is run in this session, in order, output read, results recorded verbatim in the summary and in Process 9's Review field:
      `npm run lint && npm run typecheck && npm run test:coverage && npm run build` then `npm run test:e2e && npm run test:a11y && npm audit --audit-level=high`
    - Phase-wide sweeps run and read: `git diff --stat develop..HEAD -- package.json package-lock.json` is empty (zero new dependencies across the phase); `grep -rn "dangerouslySetInnerHTML" src/ e2e/` shows only the Phase 1 lint-proof test; `grep -rn "NEXT_PUBLIC_" src/` shows nothing
    - `.planning/REQUIREMENTS.md`: UX-06, UX-07, TEST-03, TEST-04, DOC-01, DOC-02, DOC-07, DOC-08, SEC-01 checked complete; traceability table rows updated; the "Last updated" line reflects the phase close
    - `.planning/ROADMAP.md`: Phase 4 marked complete with an honest Status block in the established form (including any caveat — e.g. a measured style-src concession, or the never-run-in-CI caveat if it still applies); progress table updated
    - `.planning/STATE.md`: position updated to phase complete; the absorbed pending todos struck through with "Done in Phase 4" notes (subscribers_count README note, description:null test, coverage table, OPERATIONS gaps); the LICENSE item elevated to a flagged human decision for submission
    - `.planning/STATE.md` Blockers reconciled (plan-checker warning 3): the "No GitHub remote is configured" line is stale — PRs #10/#13/#14 merged via origin — strike it with the correction; the "defined and locally verified, not observed green in CI" caveat is replaced by the observed CI history if the phase's own PR checks have run
    - The branch is pushed and a PR into `develop` is opened (`git push -u origin feature/phase-4-quality-gate` + `gh pr create --base develop`), so DoD item 5 ("CI Gate is green on the PR") has something to gate on — the PR body carries the verification results verbatim
    - **No merge.** No `gh pr merge`, no auto-merge, no push to a protected branch. The task ends by reporting the PR URL and gate results, and stopping (D4-20)
  </acceptance_criteria>
  <action>
Write Process 9 from the four plan summaries — the whys are already recorded there with their evidence; the log entry condenses without inventing. Where a human decision must be attributed and its reason is not on record, ask the user before writing the field (AGENTS.md: a fabricated-but-plausible reason is worse than no entry). Run the gate only after every file edit in this plan is complete, so the run certifies the final tree. If any gate command fails: fix or report per AGENTS.md — a failing check is never someone else's problem — and re-run the full chain, not just the failed command.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run test:coverage && npm run build && npm run test:e2e && npm run test:a11y && npm audit --audit-level=high</automated>
  </verify>
  <done>Both logs carry identical Process 9 entries with real whys; planning docs mark the phase complete; all seven gate commands ran green in this session with output read; the agent stopped short of any merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Submission docs → reviewer trust | A README or AI log that overclaims or invents rationale poisons the whole submission's credibility |
| Agent → protected branches | The strongest temptation to merge is at phase close; authority ends at the PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Repudiation | AI usage logs / README summary | mitigate | Whys drawn only from recorded plan summaries and the human's actual statements; unknown rationale → ask, never invent (AGENTS.md rule restated in the task) |
| T-04-14 | Elevation of privilege | merge authority | mitigate | No merge step exists in any Phase 4 plan; `gh pr merge` is deny-listed in `.claude/settings.json`; the task's done-state is "report and stop" |
| T-04-15 | Tampering | gate claimed but not run | mitigate | All seven commands are the task's own automated verify; results recorded verbatim in Process 9's Review field |
| T-04-SC | Tampering | npm installs | mitigate | `git diff --stat develop..HEAD -- package.json package-lock.json` empty — the phase-wide zero-dependency proof, run and read in this task |
</threat_model>

<verification>
This plan IS the phase's terminal verification:
- The seven-command Definition of Done gate, run in order, output read
- The phase sweeps (dependency diff, dangerouslySetInnerHTML, NEXT_PUBLIC_)
- Entry-count parity across the two AI usage logs
</verification>

<success_criteria>
- DOC-01, DOC-02, DOC-07, DOC-08 closed: bilingual Japanese-first README with setup, token, decisions, and a self-contained AI summary linking both complete logs.
- OPERATIONS.md reconciled; all Phase 4 planning docs closed out; STATE.md flags LICENSE for the human.
- All seven gate commands green in this session; zero new dependencies across the phase; the agent stopped at the PR boundary.
</success_criteria>

<output>
Create `.planning/phases/04-quality-gate-submission-readiness/04-05-SUMMARY.md` when done. Record: the gate command results verbatim, the sweeps' output, any caveat carried into ROADMAP's status block, and the explicit note that the LICENSE decision and the merge are with the human.
</output>
