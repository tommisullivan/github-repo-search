---
phase: 04-quality-gate-submission-readiness
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/page.tsx
  - src/app/repos/[owner]/[repo]/page.tsx
  - src/components/SearchInput.tsx
  - src/components/ResultList.tsx
  - src/components/Pagination.tsx
  - src/components/RepoDetail.tsx
  - src/components/RepoDetail.test.tsx
  - e2e/home.a11y.spec.ts
  - e2e/search.a11y.spec.ts
  - e2e/repo-detail.a11y.spec.ts
  - e2e/keyboard.spec.ts
  - e2e/responsive.spec.ts
autonomous: true
requirements: [UX-06, UX-07]
user_setup: []

must_haves:
  truths:
    - "Every reachable render state of both views passes axe with zero violations: search blank, results, empty, rate-limit, invalid-query; detail happy, not-found"
    - "A keyboard-only Playwright journey completes search → detail → back without a mouse, with focus reaching every interactive control in a sane order"
    - "Neither view scrolls horizontally or clips content at 375×667 or 1280×800"
    - "Each page has exactly one h1 and a descending heading structure; every control has an accessible name (Japanese)"
    - "RepoDetail's description:null branch has a dedicated test (STATE.md deferred item closed)"
  artifacts:
    - path: "e2e/search.a11y.spec.ts"
      provides: "axe over the search view's five reachable states, driven through the 04-01 mock"
      contains: "fixture-ratelimit"
      min_lines: 40
    - path: "e2e/repo-detail.a11y.spec.ts"
      provides: "axe over the detail view happy path and not-found"
      contains: "repo-alpha"
    - path: "e2e/keyboard.spec.ts"
      provides: "Keyboard-only journey proof for UX-06"
      contains: "page.keyboard"
      min_lines: 30
    - path: "e2e/responsive.spec.ts"
      provides: "No-horizontal-overflow assertions at 375 and 1280 on both views"
      contains: "scrollWidth"
      min_lines: 30
    - path: "src/components/RepoDetail.test.tsx"
      provides: "description:null branch test added to the existing suite"
      contains: "description: null"
  key_links:
    - from: "e2e/search.a11y.spec.ts"
      to: "src/lib/e2e/fixtures.ts"
      via: "state selection through fixture query keywords (fixture-alpha, fixture-empty, fixture-ratelimit)"
      pattern: "fixture-"
---

<objective>
Prove — not claim — that both views are accessible and responsive, and fix what the proof finds. Axe runs against every state a reviewer can reach, a keyboard-only journey drives search → detail → back, and viewport assertions pin "usable at mobile and desktop widths" to a measurable property. Findings are fixed in components; specs are never weakened to pass.

Purpose: UX-06 and UX-07 are Phase 4 success criteria 1 and 2, and the states the current a11y spec never visits (rate-limit, results, detail) are exactly where violations hide. The 04-01 mock makes every state reachable deterministically.

Output: per-state a11y specs, a keyboard journey spec, a responsive spec, component fixes for whatever the audit finds, and the deferred `description: null` branch test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md
@.planning/phases/04-quality-gate-submission-readiness/04-01-SUMMARY.md
@docs/TESTING.md
@src/app/page.tsx
@src/components/RepoDetail.tsx
@e2e/home.a11y.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit and fix — headings, names, focus, keyboard order; close the description:null branch</name>
  <files>src/app/page.tsx, src/app/repos/[owner]/[repo]/page.tsx, src/components/SearchInput.tsx, src/components/ResultList.tsx, src/components/Pagination.tsx, src/components/RepoDetail.tsx, src/components/RepoDetail.test.tsx</files>
  <read_first>
    - Every file in <files> as it stands — the audit is against the shipped markup, not a memory of it
    - .planning/phases/04-quality-gate-submission-readiness/04-01-SUMMARY.md — any role/name query 04-01 could not satisfy is a confirmed finding, fix it first
    - .planning/STATE.md § Pending Todos — the description:null item and its exact framing ("add a fixture with description: null and assert it does not render")
  </read_first>
  <behavior>
    - Test: RepoDetail with `description: null` renders no description paragraph and no empty element where it would sit (query for the description text container's absence)
    - Existing RepoDetail/component tests all still pass unmodified — a markup fix that breaks a role/name query is a wrong fix
  </behavior>
  <acceptance_criteria>
    - Audit checklist applied to both views and recorded in the summary with per-item findings (including "none"):
      1. Exactly one `h1` per page; headings descend without skipping levels (the detail page's repo-name heading and the metrics region's sr-only heading are checked against this)
      2. Every interactive control (search input, result links, pagination previous/next, back link, error-boundary retry button) has an accessible name, in Japanese (I18N-01 applies to aria-labels)
      3. Focus is visible on every interactive control (Tailwind focus-visible styles — add where missing; never `outline-none` without a replacement)
      4. Tab order follows reading order; `aria-disabled` pagination controls remain focusable (Phase 2 D-19 kept them in the DOM deliberately — do not remove them from tab order now)
      5. The results region and state panels are reachable landmarks or headed sections — a screen-reader user can find the answer, not just the input
      6. `next/image` avatar has meaningful Japanese alt text (Phase 3 D3-12's `${owner.login} のアバター` — verify it survived)
    - Findings are fixed in the components; every fix keeps existing colocated tests green without modifying their assertions (if a test must change, the summary records why the old assertion was wrong)
    - `src/components/RepoDetail.test.tsx` gains the `description: null` case per the behavior block
    - `npm run lint`, `npm run typecheck`, `npm test` exit 0
  </acceptance_criteria>
  <action>
Work through the checklist file by file. Fixes this plan owns: heading-level corrections, missing accessible names, focus-visible styles, landmark/section structure. Fixes it does not own: copy rewrites, layout redesign, anything touching `src/lib/github/**` or `src/types/**` (sealed).

Bias to the smallest change that makes the property true. Where a component is already correct — Phase 2 and 3 were built query-by-role — record "verified, no change" rather than inventing a change. An audit that finds nothing wrong in a place is evidence, and the summary states it.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test</automated>
  </verify>
  <done>Checklist applied with findings recorded, fixes shipped in components, description:null branch tested, all unit/component tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Per-state axe specs over the mocked build</name>
  <files>e2e/home.a11y.spec.ts, e2e/search.a11y.spec.ts, e2e/repo-detail.a11y.spec.ts</files>
  <read_first>
    - e2e/home.a11y.spec.ts — the existing AxeBuilder pattern (tags, zero-violation assertion) every new spec copies
    - .planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md § D4-02a and D4-11 — the state → fixture-keyword map
  </read_first>
  <acceptance_criteria>
    - `e2e/search.a11y.spec.ts`: axe passes with zero violations on `/?q=fixture-alpha` (results), `/?q=fixture-empty` (empty state), `/?q=fixture-ratelimit` (rate-limit panel), and `/?q=fixture-alpha&page=99999` (invalid-query notice)
    - `e2e/repo-detail.a11y.spec.ts`: axe passes with zero violations on `/repos/e2e-fixture/repo-alpha` (all seven fields rendered) and `/repos/e2e-fixture/no-such-repo` (not-found page)
    - `e2e/home.a11y.spec.ts` keeps the blank-query render check; each spec waits for a state-identifying element (role/name query on the state's copy) before running axe, so axe never scans a loading fallback by accident
    - All specs use the same tags as the existing spec (`wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`) and assert `violations` equals the empty array — zero tolerance, no rule exclusions; a violation is fixed in Task 1's files (loop back), never suppressed
    - `npm run test:a11y` exits 0 with all specs listed
  </acceptance_criteria>
  <action>
One test per state, named for the state in English (`"search results state has no detectable accessibility violations"`), so a CI failure names the render that regressed. Keep specs dumb: navigate, await the state's identifying copy, run axe, assert empty. If any state fails axe, the fix belongs in Task 1's component files — apply it there, re-run `npm test` to confirm the component suite still passes, then re-run the a11y suite. Record each violation found and fixed in the summary; that list is UX-06's evidence.
  </action>
  <verify>
    <automated>npm run test:a11y</automated>
  </verify>
  <done>Seven states across three spec files pass axe with zero violations against the production build.</done>
</task>

<task type="auto">
  <name>Task 3: Keyboard-only journey and responsive assertions</name>
  <files>e2e/keyboard.spec.ts, e2e/responsive.spec.ts</files>
  <read_first>
    - .planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md § D4-12, D4-13 — what each spec must prove and what it deliberately does not
    - src/components/SearchInput.tsx — the debounce (300ms) and router.replace behaviour the keyboard spec types against
  </read_first>
  <acceptance_criteria>
    - `e2e/keyboard.spec.ts`, using only `page.keyboard` after the initial goto:
      1. From `/`, Tab reaches the search input; type `fixture-alpha`; the URL updates after the debounce (assert with Playwright's URL expectation, not a timeout)
      2. Tab from the input reaches the first result link (recording any intermediate stops); Enter navigates to `/repos/e2e-fixture/repo-alpha?from=…`
      3. On the detail page, Tab reaches the 「戻る」 link; Enter returns to the search URL with `q` and page intact
      4. At each stop, the focused element is asserted by role/accessible name (`page.evaluate(() => document.activeElement…)` or Playwright's `toBeFocused`)
    - `e2e/responsive.spec.ts`, at viewports 375×667 and 1280×800, for both `/?q=fixture-alpha` and `/repos/e2e-fixture/repo-alpha`:
      1. `document.documentElement.scrollWidth <= document.documentElement.clientWidth` — no horizontal overflow
      2. The page's key controls (search input / result links on search; back link and metrics region on detail) are visible within the viewport width
    - Overflow or clipping findings are fixed in Task 1's component files (Tailwind: `break-words`/`min-w-0`/`flex-wrap` class-level fixes), and the fix is named in the summary
    - `npm run test:e2e` exits 0 (both new files run in the E2E suite, not the a11y suite)
  </acceptance_criteria>
  <action>
The keyboard spec is UX-06's "fully keyboard operable" made falsifiable; the responsive spec is UX-07's "without horizontal scrolling or clipped content" made falsifiable. Long unbroken strings are the realistic overflow risk (repo full names, URLs in descriptions) — the fixture's repo-alpha description should include one long unbroken token so the 375px assertion actually exercises wrapping; extend `src/lib/e2e/fixtures.ts` only if the current description is too tame (coordinate: that file belongs to 04-01, which has executed — a small additive change is in-bounds and recorded).
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run test:e2e && npm run test:a11y && npm test</automated>
  </verify>
  <done>Keyboard-only journey and both-viewport overflow assertions pass against the production build; any fixes shipped in components with tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture-driven states → spec assertions | Specs must assert states only fixtures can produce, or they silently test nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Repudiation | axe/keyboard evidence | mitigate | Per-state named tests in CI — UX-06/07 claims are re-proven on every PR, not asserted once in a summary |
| T-04-05 | Tampering | spec weakening to pass | mitigate | Scope fence: violations fixed in components; no axe rule exclusions, no removed assertions — a needed exception is a stop-and-report |
| T-04-SC | Tampering | npm installs | mitigate | Zero new dependencies in this plan; phase-wide check runs in 04-05's gate |
</threat_model>

<verification>
Task-scoped commands:
- `npm run lint && npm run typecheck && npm test`
- `npm run test:a11y` (per-state axe)
- `npm run test:e2e` (keyboard + responsive + 04-01's journey still green)

The full Definition of Done gate runs once, at the end of 04-05.
</verification>

<success_criteria>
- Seven axe-checked states, zero violations, enforced in CI's a11y job from now on.
- Keyboard-only search → detail → back passes; focus visible and ordered.
- No horizontal overflow at 375 or 1280 on either view.
- description:null branch covered; audit findings (including "none" entries) recorded honestly.
</success_criteria>

<output>
Create `.planning/phases/04-quality-gate-submission-readiness/04-02-SUMMARY.md` when done. Record: the full audit checklist with per-item findings, every axe violation found and its fix, every overflow fix, and the tab-stop sequence the keyboard spec observed.
</output>
