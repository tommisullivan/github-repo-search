---
phase: 04-quality-gate-submission-readiness
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - vitest.config.mts
  - docs/TESTING.md
autonomous: true
requirements: [TEST-04]
user_setup: []

must_haves:
  truths:
    - "`npm run test:coverage` prints a readable per-file coverage table — the empty-table defect from Phase 1's deferred-items.md is fixed or replaced with a working reporter, with the root cause recorded"
    - "Coverage thresholds are raised above 70 to values chosen from the measured post-Phase-4 run, and `npm run test:coverage` passes at the new floor"
    - "The thresholds demonstrably bind: a floor set above actual fails the run (verified once, then reverted)"
    - "docs/TESTING.md's coverage section states the new thresholds, the reasoning, and the fixed reporter"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Working per-file reporter + raised thresholds with a comment recording the decision"
      contains: "thresholds"
    - path: "docs/TESTING.md"
      provides: "Coverage section updated: thresholds, per-file table status, reasoning"
      contains: "TEST-04"
  key_links:
    - from: "vitest.config.mts"
      to: ".github/workflows/ci.yml quality job"
      via: "npm run test:coverage — already the CI command; raising the config raises the CI gate with zero workflow change"
      pattern: "test:coverage"
---

<objective>
Close TEST-04 in the order STATE.md demands: first make the per-file coverage table readable — because the raise is exactly the run where someone needs to see which file fell short — then raise the thresholds deliberately from measured figures, so CI fails any future change that drops coverage below a floor that means something.

Purpose: Phase 4 success criterion 4. CI's `quality` job already runs `npm run test:coverage`; the entire enforcement change is in `vitest.config.mts`, which is why this plan is small and why it runs after every Phase-4 test has landed (04-02's component tests and 04-03's csp tests move the numbers the decision reads).

Output: fixed reporter output, raised thresholds, updated docs/TESTING.md coverage section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md
@.planning/phases/01-github-api-client/deferred-items.md
@vitest.config.mts
@docs/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the empty per-file coverage table and read the real figures</name>
  <files>vitest.config.mts</files>
  <read_first>
    - .planning/phases/01-github-api-client/deferred-items.md § D-1 — the full symptom record: header and rule print with zero rows, no `All files` row, `skipFull` already ruled out by explicit re-run, summary block and threshold gating both correct, lcov.info complete. Do not re-investigate what is already ruled out
    - vitest.config.mts — the current `reporter: ["text", "lcov"]` configuration
  </read_first>
  <acceptance_criteria>
    - `npm run test:coverage` prints per-file rows (file, % stmts, % branch, % funcs, % lines) for the files under `src/` — via a fixed `text` reporter configuration, or a documented replacement that prints per-file figures to the terminal (e.g. Vitest 4's reporter options or an alternative built-in reporter); "keep reading lcov.info by hand" is not an acceptable end state
    - The root cause (or, if genuinely undeterminable inside this task's budget, the narrowed hypothesis and what was tried) is recorded in the config comment and the summary — this is a Vitest 4 behaviour worth pinning honestly either way
    - `lcov` stays in the reporter list — CI artifacts and the existing workflow depend on nothing, but the lcov file is the audit trail the docs already cite
    - The per-file figures from the fixed output are captured in the summary as the measured baseline for Task 2's decision
    - `npm run test:coverage` still passes at the current 70 thresholds; `npm run lint` and `npm run typecheck` exit 0
  </acceptance_criteria>
  <action>
Investigate with the deferred-items notes as the starting point — the likely suspects are Vitest 4's text-reporter defaults interacting with the project's `include`/`exclude` shape, reporter option shapes (`["text", { ... }]`), or a known upstream defect with a documented workaround. Timebox the archaeology: the requirement is a readable per-file view from the standard command, not a upstream bug fix. If the `text` reporter is genuinely broken in the installed version, switching the terminal reporter (and saying so in the comment) satisfies the requirement honestly.

Do not change `include`/`exclude` semantics while fixing the reporter — the 04-01 exclusions (`src/instrumentation.ts`, `src/lib/e2e/**`) and the original excludes must survive byte-for-byte or the coverage numbers silently change meaning.
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | tail -30 && npm run lint && npm run typecheck</automated>
  </verify>
  <done>The coverage command's terminal output shows per-file rows; cause or narrowed hypothesis recorded; thresholds still green at 70; measured per-file baseline captured.</done>
</task>

<task type="auto">
  <name>Task 2: Raise the thresholds deliberately and document the decision</name>
  <files>vitest.config.mts, docs/TESTING.md</files>
  <read_first>
    - .planning/phases/04-quality-gate-submission-readiness/04-CONTEXT.md § D4-10 — the target zone (90 lines / 90 statements / 85 branches / 80 functions), the "bind, don't boast" rule, and the known 0%-reported route-convention files that weigh on the choice
    - Task 1's measured per-file output — the numbers this decision is made FROM, not the Phase 3 close figures
  </read_first>
  <acceptance_criteria>
    - Thresholds in `vitest.config.mts` are raised from 70 to values chosen from the measured run: within the D4-10 target zone unless the measured aggregate makes a zone value either trivially loose (>6 points below actual on every metric — tighten) or brittle (<2 points below actual — loosen a notch); the chosen values and the actuals they were chosen against go in the config comment, replacing the "Raised as real code lands" note
    - **The gate is proven to bind:** one deliberate run with a threshold set above the measured actual, observed to FAIL, then reverted to the chosen values — command and result recorded in the summary. A gate never seen red is a gate taken on faith
    - `npm run test:coverage` passes at the final chosen thresholds
    - docs/TESTING.md § Coverage updated: the new threshold table, the per-file table now printing (with the Task 1 explanation), the "provisional" framing removed, the reasoning for the chosen floor stated (including why functions sits lower — the route-convention files that only execute inside Next's runtime), and the TEST-04 reference marked closed
    - Thresholds are never lowered below any value previously in force; `npm run lint` and `npm run typecheck` exit 0
  </acceptance_criteria>
  <action>
Pick the numbers from evidence, write the reason where the next person will look (the config comment and TESTING.md), and prove the mechanism once. The one-run red proof is cheap here and permanent in value: it converts "CI enforces a coverage threshold" from a config assertion into an observed behaviour — which is precisely the difference Phase 4 exists to close.
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | tail -15 && npm run lint && npm run typecheck && npm test</automated>
  </verify>
  <done>Thresholds raised to measured, binding values; the red-run proof recorded and reverted; TESTING.md states the new floor and its reasoning; full suite green at the new gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future PRs → coverage gate | The threshold is the only automatic defence against untested code landing after this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Repudiation | coverage claims | mitigate | Per-file table readable from the standard command; thresholds proven to bind by an observed red run, not asserted |
| T-04-12 | Tampering | quiet threshold lowering later | mitigate | Config comment records the chosen values and their evidence; scope-fence rule "never lower" restated in TESTING.md |
| T-04-SC | Tampering | npm installs | mitigate | Zero new dependencies in this plan; phase-wide check in 04-05's gate |
</threat_model>

<verification>
Task-scoped commands:
- `npm run test:coverage` (read the per-file table and the threshold result — twice: once red on purpose, once green at the final values)
- `npm run lint && npm run typecheck && npm test`

The full Definition of Done gate runs once, at the end of 04-05.
</verification>

<success_criteria>
- TEST-04 closed: CI's existing `test:coverage` command now enforces a raised, measured, binding floor.
- The per-file table prints; the Phase 1 deferred defect is closed with its cause (or honest narrowed hypothesis) recorded.
- docs/TESTING.md matches the shipped configuration.
</success_criteria>

<output>
Create `.planning/phases/04-quality-gate-submission-readiness/04-04-SUMMARY.md` when done. Record: the root cause (or hypothesis) of the empty table, the measured per-file baseline, the chosen thresholds with the actuals beside them, and the red-run proof (command + failing output line).
</output>
