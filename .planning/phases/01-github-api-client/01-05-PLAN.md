---
phase: 01-github-api-client
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - docs/ARCHITECTURE.md
  - docs/OPERATIONS.md
  - docs/TESTING.md
  - .planning/ROADMAP.md
  - eslint.config.mjs
  - src/eslint-rules.test.ts
  - docs/AI-USAGE.en.md
  - docs/AI-USAGE.ja.md
autonomous: false
requirements: [SEC-03, TEST-01, API-02, OBS-01]
user_setup: []

must_haves:
  truths:
    - "A reviewer reading docs/ARCHITECTURE.md finds the error model the code actually implements, not the one that was superseded"
    - "docs/OPERATIONS.md names the logging mechanism the code actually uses"
    - "docs/TESTING.md's client example compiles against the real client contract"
    - "The ROADMAP's Phase 1 success criteria describe the shipped failure model, so the phase cannot be signed off against a criterion it deliberately does not meet"
    - "A future cross-unit import or a dangerouslySetInnerHTML anywhere in src/ fails lint, and the rules are proven to fire by a test rather than by their presence in a config file"
    - "The full Definition of Done gate has been run in this session and its output read"
    - "Both AI usage logs carry this phase's entry, and they carry the same number of entries"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "The hybrid returned-Result / thrown-fault error model and why throwing everything was rejected"
      contains: "notFound()"
    - path: "docs/TESTING.md"
      provides: "A client mocking example and failure-mode table matching the returned-code contract"
      contains: "ok: false"
    - path: "eslint.config.mjs"
      provides: "Enforced boundary and dangerouslySetInnerHTML rules"
      contains: "no-restricted"
    - path: "src/eslint-rules.test.ts"
      provides: "Proof that each enforced rule actually reports an error on the code it forbids"
      contains: "lintText"
      min_lines: 40
    - path: "docs/AI-USAGE.ja.md"
      provides: "This phase's entry, identical in content to the English log"
    - path: "docs/AI-USAGE.en.md"
      provides: "This phase's entry, identical in content to the Japanese log"
  key_links:
    - from: "eslint.config.mjs"
      to: "src/lib/github/search.ts and repo.ts"
      via: "no-restricted-imports override forbidding the cross-unit import"
      pattern: "no-restricted-imports"
    - from: "src/eslint-rules.test.ts"
      to: "eslint.config.mjs"
      via: "new ESLint().lintText() against the project's own flat config"
      pattern: "lintText"
---

<objective>
Close Phase 1: bring the design documents and the roadmap's own success criteria into line with the model the code actually implements, turn the boundary and security rules from review conventions into lint rules that are *proven* to fire, and run the full gate.

Purpose: `docs/ARCHITECTURE.md`, `docs/OPERATIONS.md`, `docs/TESTING.md` and the ROADMAP's Phase 1 criteria were all written before the phase's decisions were locked, and four of their statements are now wrong. For a selection task where the documents are part of the deliverable, a document that contradicts the code is worse than no document — a reviewer who spots one stops trusting all of them. The roadmap matters for a different reason: it is the checklist this phase is signed off against, and a criterion describing a model the phase deliberately rejected would either be marked met dishonestly or block a correct implementation.

Output: reconciled documentation, a corrected success criterion, lint rules with a test proving they fire, a green gate whose output was read, and both AI usage logs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-github-api-client/01-CONTEXT.md
@.planning/phases/01-github-api-client/01-01-SUMMARY.md
@.planning/phases/01-github-api-client/01-02-SUMMARY.md
@.planning/phases/01-github-api-client/01-03-SUMMARY.md
@.planning/phases/01-github-api-client/01-04-SUMMARY.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md
@docs/OPERATIONS.md
@docs/TESTING.md
@docs/SECURITY.md
@eslint.config.mjs

<known_contradictions>
Four specific statements are known to be wrong after this phase. Find them precisely rather than rewriting the documents wholesale — these are good documents and a broad rewrite would lose reasoning that is still correct.

1. `docs/ARCHITECTURE.md` — the "Typed errors, distinct UI" section, and both Mermaid sequence diagrams, show `RateLimitError` / `ValidationError` / `NotFoundError` / `NetworkError` classes being **thrown** up to `error.tsx`. The phase implements the hybrid model (D-01..D-04, D-05a): rate limit, not found, and invalid query are **returned** as a `Result` failure; only transport faults and unrecognised statuses throw.
2. `docs/OPERATIONS.md` — the observability table names `pino` as the default logging mechanism, and names it again in the same row's optional-upgrade cell (`pino` → Loki). The "What gets logged" table also gives `RateLimitError` as an example `errorType`. The phase uses `console.log` with `JSON.stringify` and no dependency (D-17, D-18), and the error vocabulary is the four codes, not class names.
3. `docs/TESTING.md` — the mocking example asserts `rejects.toBeInstanceOf(RateLimitError)`, and the "Unit — the GitHub client" section and failure-mode table describe the thrown-class model. All three describe a contract that no longer exists.
4. `.planning/ROADMAP.md` — Phase 1 success criterion 2 reads "…each surface as a distinct typed error a caller can branch on, **never a raw throw**". That predates D-05a. The phase deliberately *does* throw for transport faults and unrecognised statuses, because D-04 established that production error sanitisation makes branching inside `error.tsx` unreliable — so the states needing distinct UI are returned, and the one state needing no branching is thrown. As written, the criterion can only be signed off dishonestly.
</known_contradictions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the design documents and the roadmap with the implemented model</name>
  <files>docs/ARCHITECTURE.md, docs/OPERATIONS.md, docs/TESTING.md, .planning/ROADMAP.md</files>
  <action>
Make four targeted corrections. In each case give the reasoning, not just the new fact — these documents earn their place by explaining why, and a reviewer reading them is assessing judgement.

**`docs/ARCHITECTURE.md`.** Replace the "Typed errors, distinct UI" section's table with the implemented vocabulary: the returned failure codes `RATE_LIMIT` (carrying `resetAt`), `NOT_FOUND`, and `INVALID_QUERY`, and the thrown `GitHubRequestError` carrying `NETWORK` for transport faults and unrecognised statuses. State plainly why the split exists, which is the strongest single decision in this phase: in production Next sanitises server errors before the client error boundary receives them, so branching on error type inside `error.tsx` is unreliable — per-state UI would work in development and silently degrade to a generic message after deployment, which is a bug class that only appears in production. Note that `NETWORK` is the one case where no branching is needed, because the generic retry message in `error.tsx` is the correct and complete answer for it.

Update both Mermaid sequence diagrams so the rate-limit, 422, and 404 arrows return a typed result to the page rather than throwing, and so the detail 404 path shows the page calling `notFound()` itself. Keep the transport-failure arrow throwing to `error.tsx`.

Add a row to the "Rejected alternatives" table: throwing every failure, rejected because production error sanitisation makes the type unavailable to `error.tsx`, so the distinct rate-limit and empty-result states the brief cares about would collapse into one generic message once deployed.

Update the directory-layout listing so the `src/lib/github/` and `src/types/github.ts` rows read **Built** rather than **Planned · P1**, and add `log.ts` to it. Update the build-status prose at the top of the document accordingly, and change any node classed as `planned` in the Mermaid diagrams that is now built.

**`docs/OPERATIONS.md`.** Three edits, and the second is the one that is easy to miss:

- Change the *default* logging cell from `Structured JSON to stdout via pino` to `console.log` with `JSON.stringify`, and record the reason: AGENTS.md requires preferring the platform over a library, a read-only app with one upstream needs no levels, transports, or redaction machinery, and Next already writes stdout — so the dependency would buy nothing (D-17, D-18).
- **Rewrite the *optional upgrade* cell in the same row.** It currently reads `pino` → Loki, which is still a `pino` reference and still wrong: the upgrade path does not require adopting a logging library. Rewrite it as *a log shipper → Loki, viewed in Grafana* — stdout JSON is already shippable, so the upgrade is an infrastructure change, not a code change. That is a better story than the original and it happens to be the true one. After this edit, `grep -i pino docs/OPERATIONS.md` must return nothing.
- In the "What gets logged" field table, replace the `errorType` example `RateLimitError` with the actual vocabulary (`NETWORK`, or the timeout classification `toRequestError` produces), mark `route` as deferred until Phase 2 has routes to name, and describe `requestId` as generated per GitHub call in Phase 1, becoming per inbound request when routes exist. Do not leave the table describing fields the code does not emit.

Leave the measured fetch-cache subsection from plan 01-02 and the `#### Confirmed against the shipped client` subsection from plan 01-03 in place, and reference them from the `cacheHit` row. If plan 01-02's outcome amended the resilience table's Cancellation and Timeout rows, leave those amendments alone — they are measurements, not drafts.

**`docs/TESTING.md`.** Rewrite the Vitest mocking example so it asserts on the returned result — a 403 with an exhausted rate limit yields `ok: false` with code `RATE_LIMIT` — and add one line showing that a transport fault is the case that rejects. In the "Unit — the GitHub client" section, replace the list of error class names with the returned-code vocabulary plus the thrown `GitHubRequestError`, and state the dual contract explicitly: the capability units both return a `Result` **and** may throw, so a caller handling only `ok: false` is incomplete. Keep the sentence about the whitespace-only query being rejected before a request, which is still exactly right. In the "Failure paths are mandatory" table, update the "Must be proven" column for rate limit, not found, and validation to name returned codes, and keep the network row as a throw. Preserve the two closing assertions about rate limit never rendering as "no results" and no stack trace reaching the DOM — both are still the point.

**`.planning/ROADMAP.md`.** Reword Phase 1 success criterion 2 so it describes the shipped model. Replace the "never a raw throw" clause; the criterion should say that rate limit, not found, and validation surface as distinct machine-readable codes in a **returned** `Result` the caller branches on, while network faults and unrecognised statuses surface as a **thrown** `GitHubRequestError` carrying `NETWORK` that reaches `error.tsx` — and it should carry the one-line reason (production error sanitisation makes `error.tsx` branching unreliable, so the states needing distinct UI must not depend on it). Change nothing else in the roadmap: this is a correction to one criterion, not a re-scoping of the phase, and criteria 1 and 3–9 are all still exactly right.

Do not touch `docs/SECURITY.md`: everything it says about Phase 1 (URL construction, token handling) is what was implemented.
  </action>
  <verify>
    <automated>grep -q "notFound()" docs/ARCHITECTURE.md && grep -q "ok: false" docs/TESTING.md && ! grep -qi "pino" docs/OPERATIONS.md && ! grep -q "RateLimitError" docs/TESTING.md docs/ARCHITECTURE.md docs/OPERATIONS.md && ! grep -q "never a raw throw" .planning/ROADMAP.md && grep -q "GitHubRequestError" .planning/ROADMAP.md</automated>
  </verify>
  <done>All three documents and the roadmap's criterion 2 describe the implemented model; the rejected-alternatives table records why throwing everything was rejected; no reference to `pino` survives in either column of the observability table; no reference to the superseded error classes remains anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn the boundary and security rules into lint rules, and prove each one fires</name>
  <files>eslint.config.mjs, src/eslint-rules.test.ts</files>
  <behavior>
    - Linting a `.tsx` snippet containing `dangerouslySetInnerHTML={{ __html: "x" }}` at filePath `src/app/rule-probe.tsx` reports at least one **error** (severity 2), from either `no-restricted-syntax` or `react/no-danger`.
    - Linting an equivalent snippet **without** `dangerouslySetInnerHTML` at the same filePath reports no errors — so the first test is proving a rule, not proving that everything fails at that path.
    - Linting `import { getRepository } from "./repo";` at filePath `src/lib/github/search.ts` reports a `no-restricted-imports` error, and the same for `import { searchRepositories } from "./search";` at `src/lib/github/repo.ts`.
    - Linting `import { githubFetch } from "@/lib/github/client";` at filePath `src/app/page.tsx` reports a `no-restricted-imports` error.
    - Linting `import type { GitHubFailure } from "@/lib/github/errors";` at filePath `src/app/page.tsx` reports **no** error — Phase 2 needs that type to render its states, and a rule that blocks it would be found by Phase 2 as an obstacle and deleted wholesale, taking the `client` restriction with it.
  </behavior>
  <action>
The ARCHITECTURE.md boundary table calls itself "a code-review checklist, not decoration", and SECURITY.md bans `dangerouslySetInnerHTML` with the note "enforced by review". Review does not run in CI. Make both enforceable now, while the codebase is small enough that the rules pass on the first run.

Add to `eslint.config.mjs`, using flat-config entries and **no new dependency** (`eslint` itself is already a devDependency, and `eslint-config-next` already brings the React plugin):

1. A global `no-restricted-syntax` rule matching the JSX attribute `dangerouslySetInnerHTML` — the AST selector form `JSXAttribute[name.name="dangerouslySetInnerHTML"]` — with a message pointing at `docs/SECURITY.md`. If the selector does not match, fall back to enabling `react/no-danger`, which `eslint-config-next` already provides. The test below decides which one you keep; do not decide it by reading the plugin source.
2. A `files: ["src/lib/github/search.ts"]` entry with `no-restricted-imports` forbidding `./repo` and `@/lib/github/repo`, and the mirror entry for `repo.ts` forbidding `./search` and `@/lib/github/search`. Message: the two capability units are independent by design — see the boundary table in `docs/ARCHITECTURE.md`.
3. A `files: ["src/components/**", "src/app/**"]` entry with `no-restricted-imports` forbidding `@/lib/github/client` **only**. Do not restrict `@/lib/github/errors`: Phase 2 needs `GitHubFailure` to render its states, and a rule that blocks legitimate work gets deleted in full by whoever it blocks — taking the `client` restriction with it. Put that reasoning in a comment beside the rule so a future phase widens it deliberately rather than removing it in frustration.

**Then prove the rules fire, with a test rather than with a scratch file.** `grep -c "no-restricted" eslint.config.mjs` was the previous check and it is worthless: it passes on a rule that never matches anything, which is exactly how a lint rule fails. A rule that does not fire is worse than no rule, because it reads as covered.

Create `src/eslint-rules.test.ts`, opening with `// @vitest-environment node` (the default jsdom environment buys nothing here and the ESLint Node API wants plain Node) and a header comment explaining why this test is not colocated with a `src/` subject: its subject is `eslint.config.mjs` at the repository root, and Vitest's `include` is scoped to `src/**`, so this is the closest honest home for it.

Use ESLint's Node API against the project's own flat config — no fixture config, because the thing under test is the config that actually ships:

- `import { ESLint } from "eslint";`
- one `new ESLint()` instance reused across tests; with ESLint 9 it resolves `eslint.config.mjs` from the working directory
- `const [result] = await eslint.lintText(code, { filePath: "src/app/rule-probe.tsx" })` — the file need not exist on disk; `filePath` only decides which config entries apply, which is precisely the mechanism being tested
- assert on `result.messages`: the presence or absence of a message with `severity === 2` and the expected `ruleId`

Write one test per `<behavior>` bullet. The two negative cases — the clean JSX snippet, and the `errors` type import — are not padding. They are what distinguishes "the rule fires on the forbidden thing" from "everything is an error at this path", and without them a misconfigured `files` glob would look like success.

Run `npm run lint` and confirm the existing tree still passes. If a rule trips on code written earlier in this phase, that is the rule working — fix the code, not the rule. Do not add an `eslint-disable` anywhere; AGENTS.md forbids disabling a rule to make an error go away.
  </action>
  <verify>
    <automated>npm run lint && npm test -- src/eslint-rules.test.ts && npm run typecheck</automated>
  </verify>
  <done>`eslint.config.mjs` carries the three rule groups, `npm run lint` passes on the current tree, `src/eslint-rules.test.ts` proves each rule reports an error on the code it forbids **and** stays silent on the code it must permit, and no `eslint-disable` was added anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full gate, Phase 1 sweeps, AI usage logs, and human sign-off</name>
  <files>docs/AI-USAGE.en.md, docs/AI-USAGE.ja.md</files>
  <action>Run the full Definition of Done gate and the Phase 1 sweeps listed below, read every output, append this phase's entry to both AI usage logs, then present the results and stop for the human decision described in what-built.</action>
  <what-built>
Phase 1 is code-complete: `src/types/github.ts`, `src/lib/github/{errors,log,client,search,repo}.ts` with colocated tests, a measured and confirmed fetch-cache configuration, reconciled design documents, a corrected roadmap criterion, and enforced boundary lint rules with a test proving they fire.

Before this checkpoint, run the **full** gate from a shell where `nvm use` has been run, and read every output rather than skimming for a green tick:

`npm run lint`, then `npm run typecheck`, then `npm run test:coverage`, then `npm run build`, then `npm run test:e2e`, then `npm run test:a11y`, then `npm audit --audit-level=high`.

All seven, not the first four. The Definition of Done names `test:e2e` and `test:a11y` explicitly, and a phase that skips them has not met it however green the rest looks. `npm run test:coverage` is the one `npm test` does not cover — the CI `quality` job gates on it, and Phase 1 has just added the first real source files, so the 70% thresholds are meeting real code for the first time. Do not raise the thresholds: that is Phase 4 work under TEST-04, and raising them here would trip over the decision rather than making it.

Also run and read these Phase 1 sweeps:
- `grep -rn "api.github.com" src/` — `client.ts` only.
- `grep -rn "GITHUB_TOKEN" src/` — `client.ts` only.
- `grep -rn "NEXT_PUBLIC" src/` — nothing.
- `grep -rn "127.0.0.1" src/` — nothing. Plan 01-03's temporary confirmation edit was reverted.
- `grep -rn "dangerouslySetInnerHTML" src/ e2e/` — nothing (SEC-03).
- `grep -n "force-cache" src/lib/github/client.ts` — present. This is API-05's opt-in, and its absence is silent and expensive.
- **No user-facing prose in the client layer** — the Japanese-character check in the automated chain below, over `src/lib/github/*.ts` and `src/types/*.ts`, excluding tests. D-05 and D-06 put every Japanese string in the UI layer; a Japanese character in these modules means presentation leaked into the client and Phase 2 will end up maintaining copy in two places.
- `grep -rnE "@ts-ignore|@ts-expect-error" src/lib/github/ src/types/` — nothing. Bare `any` is deliberately not grepped for: the English word appears legitimately in comments, and `@typescript-eslint/no-explicit-any` already fails `npm run lint` on the real thing.
- `git diff --stat package.json package-lock.json` — empty. Zero new production dependencies across the whole phase.

Then append this phase's entry to **both** `docs/AI-USAGE.ja.md` and `docs/AI-USAGE.en.md` using the template in AGENTS.md, as the next sequential process number — `## 工程 6:` in the Japanese file and `## Process 6:` in the English one, matching the heading style each file already uses. The fields that carry the weight here are *why*, and they must be the reasons actually recorded in `01-CONTEXT.md` and in the plan summaries, not reconstructed ones: the hybrid failure model and the production error-sanitisation reasoning behind it, `console.log` over `pino`, the two different revalidate windows, `per_page=20`, `subscribers_count` over `watchers_count`, and — the one a reviewer will find most telling — that the fetch-cache configuration was **measured** rather than assumed, after the first draft of these plans specified options that Next 16 does not cache. Record the rejected alternatives with their reasons: throwing every failure, adding a logging dependency, and making the API base URL configurable by environment variable. If you are unsure why a choice was made, ask rather than inventing a plausible reason.
  </what-built>
  <how-to-verify>
1. Read the gate output above. All seven commands green, and `npm run test:coverage` showing coverage of `src/lib/github/` — the error branches are the point, so check they are covered rather than trusting the aggregate percentage.
2. Read `src/lib/github/repo.ts` at the watchers mapping. Confirm it reads from `subscribers_count` and that the comment explains the trap where you are looking, not elsewhere.
3. Read one log line: read `client.test.ts`'s asserted log fields and confirm no token, no `Authorization`, and no response body appears.
4. Read the measured fetch-cache subsection in `docs/OPERATIONS.md` **and the `#### Confirmed against the shipped client` subsection below it**. Confirm the conclusion is supported by the request counts recorded there, that the confirmation ran through the real `githubFetch` rather than a hand-written fetch, and that the log line does not claim live headroom the measurement contradicts. This is the phase's only evidence for API-05 and ROADMAP criterion 5 — a passing unit suite does not substitute for it.
5. Read both AI usage entries side by side. Confirm they say the same thing, that every *why* is a reason you actually gave, and that the rejected alternatives carry their reasons.
6. Decide whether Phase 1 is ready to open a pull request into `develop`. Per AGENTS.md the agent opens the PR and stops — it never merges, and green CI alone is not sufficient.
  </how-to-verify>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run test:coverage && npm run build && npm run test:e2e && npm run test:a11y && npm audit --audit-level=high && test -z "$(git diff --name-only package.json package-lock.json)" && ! grep -rn "dangerouslySetInnerHTML" src/ e2e/ && ! grep -rn "127.0.0.1" src/ && grep -q "force-cache" src/lib/github/client.ts && node -e "const fs=require('fs'),path=require('path');const dirs=['src/lib/github','src/types'];const bad=[];for(const d of dirs){for(const f of fs.readdirSync(d)){if(!f.endsWith('.ts')||f.endsWith('.test.ts'))continue;const t=fs.readFileSync(path.join(d,f),'utf8');const m=t.match(/[　-ヿ一-鿿＀-￯]/g);if(m)bad.push(d+'/'+f+': '+[...new Set(m)].join(''))}}if(bad.length){console.error('User-facing prose in a presentation-free module:',bad.join(' | '));process.exit(1)}" && [ "$(grep -c '^## 工程' docs/AI-USAGE.ja.md)" = "$(grep -c '^## Process' docs/AI-USAGE.en.md)" ] && [ "$(grep -c '^## 工程' docs/AI-USAGE.ja.md)" -ge 6 ]</automated>
  </verify>
  <done>Every gate command ran in this session and its output was read; the Phase 1 sweeps return what they should; both AI usage logs carry this phase's entry and hold the same number of entries; the human has approved or named what must change.</done>
  <resume-signal>Type "approved" to close Phase 1, or describe what needs to change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository source → CI lint job | Rules that exist only in prose are not enforced; rules in `eslint.config.mjs` are — but only if they actually match |
| Documentation → reviewer's trust | A document contradicting the code misleads the one audience this project has |
| Planning artifacts → phase sign-off | A success criterion describing an unimplemented model is either signed off dishonestly or blocks correct work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering | Future XSS via `dangerouslySetInnerHTML` | mitigate | Lint rule fails the build repo-wide, and `src/eslint-rules.test.ts` proves it reports an error on a real snippet — a config-file grep would pass on a rule that never matches (SEC-03) |
| T-01-19 | Elevation of privilege | A route or component calling `githubFetch` directly and bypassing the units' guards | mitigate | `no-restricted-imports` on `src/app/**` and `src/components/**` for `@/lib/github/client`, with a test proving it fires and a matching test proving the `errors` type import is still allowed, so the rule is not deleted wholesale by the first phase it obstructs |
| T-01-20 | Information disclosure | Token, `NEXT_PUBLIC_`, or a leftover local base URL introduced during the phase | mitigate | Explicit grep sweep in the checkpoint over `src/` for `GITHUB_TOKEN`, `NEXT_PUBLIC`, and `127.0.0.1`, the last covering plan 01-03's temporary confirmation edit |
| T-01-21 | Repudiation | Undocumented AI involvement in the delivered code | mitigate | Both AI usage logs updated with the reasoning and the rejected alternatives before the phase closes. The automated check compares entry counts across the two files against their real heading styles (`## 工程` and `## Process`) rather than grepping for an English word that never appears in the Japanese file |
| T-01-28 | Denial of service | API-05 quietly unmet, surfacing later as rate-limit exhaustion | mitigate | The checkpoint reads the counted confirmation from plan 01-03 rather than accepting a passing unit suite, and the automated chain greps `client.ts` for the cache opt-in |
| T-01-SC | Tampering | npm installs | mitigate | The phase adds zero production dependencies; `git diff --stat package.json package-lock.json` is checked as part of the gate. Any package addition is a blocking human decision, never an executor's |
</threat_model>

<verification>
The full Definition of Done gate from `.planning/ROADMAP.md`, run after `nvm use` and read in full:

`npm run lint && npm run typecheck && npm run test:coverage && npm run build`
`npm run test:e2e && npm run test:a11y && npm audit --audit-level=high`

Plus the Phase 1 sweeps listed in the checkpoint, and both AI usage logs updated with matching entry counts.

Shipping is a separate step. This plan ends at a human decision; the agent never merges.
</verification>

<success_criteria>
- No design document and no roadmap criterion contradicts the implemented error model, logging mechanism, or build status.
- The boundary rules and the `dangerouslySetInnerHTML` ban are enforced by lint, and each rule is proven to fire by an automated test rather than by its presence in a config file (SEC-03).
- `npm run test:coverage` passes with the existing 70% thresholds unchanged, and `src/lib/github/` error branches are covered (TEST-01).
- `npm run test:e2e` and `npm run test:a11y` both ran and passed — the Definition of Done requires them and they are in the automated chain.
- No Japanese text appears in `src/lib/github/` or `src/types/` outside test files (D-05, D-06).
- Zero new production dependencies across the entire phase.
- Both AI usage logs carry an entry whose *why* matches the recorded decisions, and both files hold the same number of entries.
- A human has reviewed the phase and said whether it is ready for a pull request.
</success_criteria>

<output>
Create `.planning/phases/01-github-api-client/01-05-SUMMARY.md` when done. Record the coverage numbers for `src/lib/github/` as measured, the counted cache-confirmation result carried forward from plan 01-03 as the phase's API-05 evidence, and list any Phase 1 success criterion that is defined-and-locally-verified rather than observed in CI, in the same honest form Phase 0 used.
</output>
